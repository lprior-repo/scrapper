# GitHub Configuration
GITHUB_TOKEN=your_github_token_here
GITHUB_ORG=microsoft
GITHUB_MAX_REPOS=100
GITHUB_MAX_TEAMS=50
GITHUB_OUTPUT_FILE=scan_results.json

# Database Configuration
NEO4J_URI=bolt://localhost:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=password

# Application Configuration
ENVIRONMENT=development
HTTP_PORT=8081
LOG_LEVEL=DEBUG

# GoFr Observability Configuration
# Distributed tracing with Jaeger integration
# TRACE_EXPORTER: Exporter type for distributed tracing (jaeger, stdout, noop)
# TRACER_RATIO: Sampling ratio for traces (0.0-1.0). Use 1.0 for development, lower for production
# JAEGER_ENDPOINT: OpenTelemetry-compatible Jaeger endpoint for trace ingestion
# TRACER_URL: Alternative URL configuration for trace exporter (required by some GoFr versions)
TRACE_EXPORTER=jaeger
TRACER_RATIO=1.0
JAEGER_ENDPOINT=http://localhost:4318/v1/traces
TRACER_URL=http://localhost:4318/v1/traces

# Metrics configuration for Prometheus-compatible metrics
# ENABLE_METRICS: Enable application metrics collection (true/false)
# METRICS_PORT: Port for metrics endpoint exposure (default 2121)
ENABLE_METRICS=true
METRICS_PORT=2121

# Legacy GoFr telemetry (disable when using new observability stack)
GOFR_TELEMETRY=false

# Optional Configuration
CLONE_CONCURRENCY=50
API_RATE_LIMIT=5000
SCAN_TIMEOUT=300